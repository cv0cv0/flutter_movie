class Movie {
  Movie(this.title, this.average, this.collectCount, this.smallImage,
      this.director, this.cast, this.movieId,
      {this.summary});

  final title;
  final average;
  final collectCount;
  final smallImage;
  final director;
  final cast;
  final movieId;
  final summary;
}
